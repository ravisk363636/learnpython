Open `index.html` in a browser for the clickable screen map.

To generate Figma frames: create a new Figma file, Plugins → Development → Import plugin from manifest, select `figma-plugin/manifest.json`, then run **QueueLite UI Generator**.
